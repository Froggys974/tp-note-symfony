App\Entity\Participant:
  participant_{1..20}:
    event: '@event_<numberBetween(1, 10)>'
    user: '@user_<numberBetween(1, 10)>'
    status: "<randomElement(['registered', 'cancelled', 'present', 'absent'])>"
    registrationDate: <dateTimeBetween('-1 years', 'now')>