<header class="bg-black bg-opacity-90 sticky top-0 z-50 shadow-lg">
  <nav class="container mx-auto flex items-center justify-between py-4 px-6">
    <a href="{{ path('app_home') }}" class="text-3xl font-extrabold tracking-wide text-red-600 hover:text-red-700">Eventia</a>

    {% if app.user %}
      <div class="text-white mx-4 hidden md:block">
        Bonjour {{ app.user.userProfile.firstName ?? '' }} {{ app.user.userProfile.lastName ?? '' }}
      </div>
    {% endif %}

    {% include 'parts/_navbar.html.twig' %}
  </nav>
</header>
